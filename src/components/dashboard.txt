import React, { useEffect, useState } from 'react';
import {
  Grid,
  Card,
  CardContent,
  Typography,
  Button,
  Box,
} from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { SportType } from '../types/types';
import { useAuth } from '../contexts/AuthContext';

const Dashboard: React.FC = () => {
  const navigate = useNavigate();
  const { user } = useAuth();
  const [activeGames, setActiveGames] = useState([]);
  const [upcomingFixtures, setUpcomingFixtures] = useState([]);

  useEffect(() => {
    // Fetch active games and upcoming fixtures
    // This will be implemented later
  }, []);

  const sportCards = Object.values(SportType).map((sport) => (
    <Grid item xs={12} sm={6} md={4} key={sport}>
      <Card>
        <CardContent>
          <Typography variant="h6" component="h2">
            {sport.replace(/_/g, ' ')}
          </Typography>
          <Box sx={{ mt: 2 }}>
            <Button
              variant="contained"
              color="primary"
              onClick={() => navigate(`/scoreboard/${sport}`)}
            >
              Start New Game
            </Button>
          </Box>
        </CardContent>
      </Card>
    </Grid>
  ));

  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4" gutterBottom>
        Dashboard
      </Typography>
      
      <Typography variant="h6" gutterBottom>
        Available Sports
      </Typography>
      <Grid container spacing={3}>
        {sportCards}
      </Grid>

      <Typography variant="h6" gutterBottom sx={{ mt: 4 }}>
        Active Games
      </Typography>
      <Grid container spacing={3}>
        {/* Active games will be displayed here */}
      </Grid>

      <Typography variant="h6" gutterBottom sx={{ mt: 4 }}>
        Upcoming Fixtures
      </Typography>
      <Grid container spacing={3}>
        {/* Upcoming fixtures will be displayed here */}
      </Grid>
    </Box>
  );
};

export default Dashboard;